diff --git a/objects/obj_base_enemy/Create_0.gml b/objects/obj_base_enemy/Create_0.gml
index 08b09a1..b923fd1 100644
--- a/objects/obj_base_enemy/Create_0.gml
+++ b/objects/obj_base_enemy/Create_0.gml
@@ -20,6 +20,8 @@ damage_time = 0.1;
 damage_timer = 0;
 
 death_effect_index = -1;
+// Gold spawned when this enemy is killed (0 = none).
+gold_drop = 0;
 death_effect_size = 1;
 delayed_death_effect = false;
 
diff --git a/objects/obj_base_enemy/Destroy_0.gml b/objects/obj_base_enemy/Destroy_0.gml
index 2e33e95..3b43e2f 100644
--- a/objects/obj_base_enemy/Destroy_0.gml
+++ b/objects/obj_base_enemy/Destroy_0.gml
@@ -18,6 +18,12 @@ if ((hp <= 0 || global.enemy_kill_signal) && death_effect_index > -1)
 	if (audio_exists(sound_dead)) play_sound(sound_dead, 1, false, 1, sound_pitch,0);
 }
 
+// Drop gold on death.
+if ((hp <= 0 || global.enemy_kill_signal) && gold_drop > 0)
+{
+	_scr_gbj14_spawn_gold(gold_drop, x, y, OBJECT_DEPTHS.PLAYER + 10);
+}
+
 if (global.enemy_kill_signal) dying = true;
 if (is_boss) global.enemy_kill_signal = true;
 if (death_next_enemy_index > -1)
diff --git a/objects/obj_gbj14_enemy_boss_real/Create_0.gml b/objects/obj_gbj14_enemy_boss_real/Create_0.gml
index ff44475..1290b3e 100644
--- a/objects/obj_gbj14_enemy_boss_real/Create_0.gml
+++ b/objects/obj_gbj14_enemy_boss_real/Create_0.gml
@@ -17,4 +17,9 @@ shot_count = 1;
 
 is_boss = true;
 hp_max = 5;
-hp = hp_max;
\ No newline at end of file
+hp = hp_max;
+
+// Death poof and gold reward: bigger poof and a big payout for bosses.
+death_effect_index = spr_effect_ring_large;
+death_effect_size = 2;
+gold_drop = 25;
diff --git a/objects/obj_gbj14_enemy_ghost/Create_0.gml b/objects/obj_gbj14_enemy_ghost/Create_0.gml
index 998a765..2ed924c 100644
--- a/objects/obj_gbj14_enemy_ghost/Create_0.gml
+++ b/objects/obj_gbj14_enemy_ghost/Create_0.gml
@@ -16,4 +16,8 @@ axis_max_speed = r3(20, 20, 0);
 
 anim_speed = 0.15;
 
-apply_gravity_force = false;
\ No newline at end of file
+apply_gravity_force = false;
+
+// Death poof and gold reward.
+death_effect_index = spr_effect_ring_large;
+gold_drop = 5;
diff --git a/objects/obj_gbj14_enemy_shuffle/Create_0.gml b/objects/obj_gbj14_enemy_shuffle/Create_0.gml
index ede4e5e..beb9126 100644
--- a/objects/obj_gbj14_enemy_shuffle/Create_0.gml
+++ b/objects/obj_gbj14_enemy_shuffle/Create_0.gml
@@ -8,4 +8,8 @@ hp_max = 1;
 hp = hp_max;
 
 velocity[0] = -15;
-axis_max_speed = r3(50, 1200, 0);
\ No newline at end of file
+axis_max_speed = r3(50, 1200, 0);
+
+// Death poof and gold reward.
+death_effect_index = spr_effect_ring_large;
+gold_drop = 5;
