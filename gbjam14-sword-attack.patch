diff --git a/objects/obj_gbj14_enemy_boss_real/Create_0.gml b/objects/obj_gbj14_enemy_boss_real/Create_0.gml
index 7b5c5ec..093022f 100644
--- a/objects/obj_gbj14_enemy_boss_real/Create_0.gml
+++ b/objects/obj_gbj14_enemy_boss_real/Create_0.gml
@@ -13,4 +13,9 @@ cycle_y_speed = 5;
 
 shot_time = 2.5;
 shot_timer = shot_time;
-shot_count = 1;
\ No newline at end of file
+shot_count = 1;
+
+// Bosses take 5 sword hits to defeat.
+is_boss = true;
+hp_max = 5;
+hp = hp_max;
\ No newline at end of file
diff --git a/objects/obj_gbj14_player/Create_0.gml b/objects/obj_gbj14_player/Create_0.gml
index 4ff06a1..b419cd8 100644
--- a/objects/obj_gbj14_player/Create_0.gml
+++ b/objects/obj_gbj14_player/Create_0.gml
@@ -46,6 +46,16 @@ idle_animation_timer = 0;
 idle_animation_time = 5;
 
 list_items = ds_list_create();
+scr_gbj14_player_Add_Item(self, {
+	name: "Sword",
+	price: 0,
+	uses: -1,
+	sprite:  spr_gb12_player_item_sword,
+	sound:   snd_gbj14_player_attack,
+	script: "scr_gbj14_player_Use_Item_Sword",
+	show_cursor: "",
+	description: "Slash foes and brittle walls."
+});
 scr_gbj14_player_Add_Item(self, {
 	name: "Lift",
 	price: 0,
@@ -68,6 +78,7 @@ item_throw = {
 	description: "Throw objects"
 };
 damage = 1;
+attack_hitbox_id = noone;
 
 // Set up physics
 axis_max_speed = r3(120, 1200, 0);
diff --git a/objects/obj_gbj14_player/Destroy_0.gml b/objects/obj_gbj14_player/Destroy_0.gml
index a156f53..b58d50c 100644
--- a/objects/obj_gbj14_player/Destroy_0.gml
+++ b/objects/obj_gbj14_player/Destroy_0.gml
@@ -6,6 +6,9 @@ event_inherited();
 
 ds_list_destroy(list_items);
 
+if (attack_hitbox_id != noone && instance_exists(attack_hitbox_id))
+	instance_destroy(attack_hitbox_id);
+
 // If the player is destroyed due to HP being 0, spawn a corpse.
 if (global.player_hp <= 0)
 {
diff --git a/objects/obj_gbj14_player/Draw_73.gml b/objects/obj_gbj14_player/Draw_73.gml
index 8b0b06f..64344b0 100644
--- a/objects/obj_gbj14_player/Draw_73.gml
+++ b/objects/obj_gbj14_player/Draw_73.gml
@@ -75,4 +75,14 @@ else
 			scr_change_sprite(spr_gbj14_player_lift_idle);
 		}
 	}
+	else if (action == 2)
+	{
+		scr_change_sprite(spr_gbj14_player_sword);
+		anim_speed = 0.3;
+		if (anim_looped != 0)
+		{
+			action = 0;
+			scr_change_sprite(spr_gbj14_player_idle);
+		}
+	}
 }
\ No newline at end of file
diff --git a/objects/obj_gbj14_player/Step_0.gml b/objects/obj_gbj14_player/Step_0.gml
index 5d302b0..2a87fba 100644
--- a/objects/obj_gbj14_player/Step_0.gml
+++ b/objects/obj_gbj14_player/Step_0.gml
@@ -84,6 +84,19 @@ if (!paused)
 			can_move = false;
 			can_act = false;
 		}
+		// Attacking with the sword
+		if (action == 2)
+		{
+			can_move = false;
+			can_act = false;
+		}
+	}
+	
+	// Clean up the sword hitbox once the swing ends or is interrupted (e.g. by getting hurt).
+	if (action != 2 && attack_hitbox_id != noone)
+	{
+		if (instance_exists(attack_hitbox_id)) instance_destroy(attack_hitbox_id);
+		attack_hitbox_id = noone;
 	}
 
 	// Keep a short jump window after leaving the ground so jumps do not
diff --git a/rooms/rm_gbj14_map/InstanceCreationCode_collide_break.gml b/rooms/rm_gbj14_map/InstanceCreationCode_collide_break.gml
new file mode 100644
index 0000000..46f1c73
--- /dev/null
+++ b/rooms/rm_gbj14_map/InstanceCreationCode_collide_break.gml
@@ -0,0 +1 @@
+layer_name = "tilemap_break";
\ No newline at end of file
diff --git a/scripts/scr_gbjam14_player/scr_gbjam14_player.gml b/scripts/scr_gbjam14_player/scr_gbjam14_player.gml
index 998e5c4..bb81dca 100644
--- a/scripts/scr_gbjam14_player/scr_gbjam14_player.gml
+++ b/scripts/scr_gbjam14_player/scr_gbjam14_player.gml
@@ -264,18 +264,56 @@ function scr_gbj14_player_Use_Item_Bomb()
 }
 function scr_gbj14_player_Use_Item_Sword()
 {
+	// Only start a swing from the normal state.
+	if (action != 0) return false;
+	
 	play_sound(snd_gbj14_player_attack, 1, 0, 1,1,0.1);
-	var _pos = scr_gbj14_player_Cursor_Tool();
-	if (_scr_gbj14_player_Use_Item_Dig(floor(_pos[0]/16), floor(_pos[1]/16), "tilemap_earth"))
+	action = 2;
+	scr_change_sprite(spr_gbj14_player_sword);
+	
+	var _dir = cos(degtorad(direction_facing));
+	
+	// Spawn a melee hitbox covering the swing area in front of the player.
+	// It reuses obj_shot_player so obj_base_enemy's existing shot collision
+	// handles damage, hit flash, sounds and knockback. It follows the player
+	// for the whole swing and is cleaned up by the player object when the
+	// attack state ends.
+	var _hitbox = instance_create_depth(position[0] + _dir * 14, position[1] - 12, depth + 1, obj_shot_player);
+	if (instance_exists(_hitbox))
 	{
-		if (random_range(0,100) < 10)
-			_scr_gbj14_spawn_gold(10,
-				floor(_pos[0]/16) * 16 + 8,
-				floor(_pos[1]/16) * 16 + 12,
-				OBJECT_DEPTHS.PLAYER + 10);
-		return true;
+		_hitbox.creator_ID = id;
+		_hitbox.visible = false;
+		// 12x12 rectangle mask scaled up to a 24x24 swing area.
+		_hitbox.mask_index = msk_gb12_player;
+		_hitbox.image_xscale = 2;
+		_hitbox.image_yscale = 2;
+		_hitbox.damage = damage;
+		_hitbox.piercing = true;        // survive armored/blocking enemies
+		_hitbox.hits_only_once = true;  // one hit per enemy per swing
+		_hitbox.dies_offscreen = false;
+		_hitbox.movement_enabled = false;
+		_hitbox.apply_gravity_force = false;
+		// Follow the player during the swing.
+		_hitbox.dom_id = id;
+		_hitbox.dom_offset_x = _dir * 14;
+		_hitbox.dom_offset_y = -12;
+		attack_hitbox_id = _hitbox;
 	}
-	return false;
+	
+	// The sword also breaks any tile on the breakable layer within the swing area.
+	var _left   = position[0] + _dir * 14 - 12;
+	var _right  = position[0] + _dir * 14 + 12;
+	var _top    = position[1] - 24;
+	var _bottom = position[1];
+	var _corners = {
+		_l: floor(_left / 16),
+		_r: floor(_right / 16) + 1,
+		_t: floor(_top / 16),
+		_b: floor(_bottom / 16) + 1
+	};
+	_scr_gbj14_Destroy_Tilemap_Area(_corners, "tilemap_break");
+	
+	return true;
 }
 function scr_gbj14_player_Use_Item_Shovel()
 {
diff --git a/sprites/spr_gbj14_player_sword/spr_gbj14_player_sword.yy b/sprites/spr_gbj14_player_sword/spr_gbj14_player_sword.yy
index 7e17497..b58d33b 100644
--- a/sprites/spr_gbj14_player_sword/spr_gbj14_player_sword.yy
+++ b/sprites/spr_gbj14_player_sword/spr_gbj14_player_sword.yy
@@ -126,7 +126,7 @@
     ],
     "visibleRange":null,
     "volume":1.0,
-    "xorigin":31,
+    "xorigin":16,
     "yorigin":33,
   },
   "swatchColours":null,
